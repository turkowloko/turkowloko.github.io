source "https://rubygems.org"

gem "jekyll", "~> 4.2" # Ou a versão que você instalou/deseja
gem "jekyll-seo-tag"
gem "wdm", ">= 0.1.0", :platforms => [:mingw, :mswin, :x64_mingw, :jruby] # Apenas para Windows
gem "logger" # Para silenciar o aviso do logger
gem "fiddle" # Para silenciar o aviso do fiddle/import

# Se você tiver outros plugins Jekyll, adicione-os aqui.